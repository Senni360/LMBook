# Work log

## 2026-09-14 — requested release wrap-up, 0.2.9

The owner ended the open-ended sweep and requested a new GitHub release plus a concise summary across today's workstreams. Created private repository `https://github.com/Senni360/SenniBook` because no remote or repository existed. Prepared installer, portable and SHA-256 sums under `release/0.2.9/`; publication follows this source checkpoint. Credentials, owner libraries, generated media and diagnostic artifacts remain outside Git. A bounded pattern scan of eligible working files and reachable historical text versions found no credential patterns; this is not a universal secret-detection guarantee.

Finished v3 compact backups and added three focused regressions under the explicit wrap-up test-writing exception. Small backups stay v2; repeated/changed/omitted historical source snapshots, original/audio restoration and remapped IDs pass. Malformed hashes and excessive declarations fail before original publication. All 38 application tests passed. Packaged startup/private-backend/sandbox/persistence/restart passed, along with native Markdown/original/ZIP downloads, 28 MB synthetic notebook backup/restore, failed download preserving the old destination and app screen, cancellation, partial/empty/oversize imports and source/goal filter resets. The 21 MiB document was rejected before any upload. Renderer errors were empty for the packaged download workflow; desktop/390px status panels fit. Backend, bridge and HTML match the source build; packaged backend SHA-256 is `249ad0d94fd112766e44bab463e49d21bdc520b8d88b036af13d62bffccc6367`.

The initial large-restore diagnostic read a 28 MB response through the inspector cache and hit response-body eviction; the corrected probe waits for the real completion notice and compares content inside the renderer. An earlier follow-on settings selector assumed restore stayed in Settings, but restore deliberately opens the new notebook's sources; the probe now reopens Settings before its failure-path check. These were diagnostic corrections, not app failures. No new provider or speech generation ran during this sweep; earlier authorized provider trials and local-voice research from other threads are recorded below. [Release highlights](docs/releases/0.2.9.md) distinguish shipped features from the learner-interview and local-TTS research. Further feature work waits for the owner's next instruction.

## 2026-09-14 — ongoing no-generation desktop stress test

Post-0.2.8 continuation: the create dialog now focuses its name input on opening, and the skip link focuses the main region. The keyboard interaction confirmed both, including Escape returning focus to the launch button. Source/backup selection now preflights the server's existing size limits; a synthetic 21 MiB document was rejected with a readable 20 MB limit message, zero upload requests and the earlier source retained. Partial import, search/filter reset and empty backup checks still passed. These source changes are not packaged yet. Actual interrupted-response probes against the local original, bundle and WAV routes rejected after headers, preserved existing destinations and removed owned temporary files. The earlier arbitrary malformed-HTTP200 mock did not demonstrate a production publication bug; MP3 shares the terminating pipeline by inspection but was not exercised in that fault probe. Large-backup v3 integration remains in progress.

0.2.8 checkpoint is packaged and verified: installer/portable in `release/0.2.8/`, all 35 existing application tests pass, packaged startup/sandbox/persistence/restart passes, and packaged Trash/download/import/filter workflows pass. Purge preserves shared historical originals and removes unique current/historical originals, owned audio/cache, activity rows and local draft/resume state; backend failure/retry probes passed. Packaged bridge/backend/HTML bytes match the checkpoint; backend SHA-256 is `e2bb63e5294ddc93b7b018d63dba855369d5e7becf8ede435d4e6d0a996df69f`. The owner has been given the portable link. No paid generation, owner-library writes, commits or pushes. Empty-file transport experiments were reverted because they did not improve behavior; source/ZIP preflight reliably prevents that path. Next: compact source snapshot ZIP integration, with legacy v1/v2 compatibility, full schema validation and expanded-memory limits; `server/bundle-source-snapshots.ts` is standalone and unshipped as ZIP behavior. Preserve other threads' earlier changes and the learning research workstream.

Desktop download interaction passed: Markdown, ZIP and exact original bytes saved through the new bridge; a deliberately oversized backup showed a readable error while preserving the app URL and an existing destination; cancelling the save dialog created no file. Desktop/390px status panels fit and renderer errors were empty. Latest full application run passed 35/35; version is now 0.2.8, not packaged yet. A zero-byte file exposed a custom-protocol multipart hang (direct HTTP returns 400; positive-size invalid files return normally through both paths). Client source/ZIP preflight prevents it and partial-batch/filter interactions pass; a bounded transport investigation is ongoing before packaging. A separate review's arbitrary HTTP200/partial-body probe is not yet evidence of a reachable production truncation path; actual route stream-error behavior is being checked before expanding download validation.

The latest source build passed Trash/activity cleanup and a failed-refresh creation interaction: a saved notebook now opens immediately even when refreshing the sidebar fails; ZIP restore uses the same path. Source batch errors now identify the failed filename and previously saved count. Library summaries moved into a separate SQLite cache table maintained by triggers, preserving old two-column notebook writers; migration/restart, legacy SQL updates, and trash/restore diagnostics passed. Existing 35 tests passed in the contributor checks. Desktop downloads are being changed to a narrow streaming bridge with native save selection, cancellation and a persistent status/error panel; end-to-end download inspection is pending. A v3 source-snapshot module is being developed independently for the later large-backup fix and is not yet integrated or claimed as functional ZIP support.

Trash source-build interaction passed: move retained all originals/audio/cache; restore retained the original ID and unsaved draft without changing another active notebook; permanent-delete cancellation preserved everything; confirmed purge removed unique current and historical originals, final audio/cache and local drafts/resume positions while preserving an original referenced only by another notebook's episode snapshot. Narrow/desktop Trash screenshots had no horizontal overflow and 44px action targets; renderer errors were empty. A successful move followed by a simulated list-refresh failure now clears the deleted selection and explains the outcome. Further hardening covers interrupted audio artifacts, persistent purge failure/retry and notebook activity cleanup. Not packaged yet. Synthetic library/backup measurements found unnecessary full-library JSON parsing and a 20 MiB manifest ceiling reached by a 5 MiB source pack plus three repeated snapshots. Download failure handling is being independently investigated; no real generation.

0.2.7 packaging completed: installer/portable in `release/0.2.7`, packaged smoke and full no-generation UI workflow passed, and bundled backend SHA matches the checkpoint. The owner confirmed recoverable Trash plus explicit permanent deletion. Root is implementing Trash UI while Luna owns backend transactions/shared-file cleanup and import serialization. No automatic orphan cleanup. Further draft diagnostics passed (empty description, newer draft versus delayed save, overlong preparation retention, double-submit guard). Source/goal filter empty states and accessible selection/expansion are also being refined. The next build must not claim Trash complete until restore, shared-file purge, failure recovery and packaged UI behavior have been verified.

Second checkpoint: extended draft recovery to episode setup, provider/model, notebook details and per-episode voices; retained episode/chapter selection and playback speed; aligned automatic/media next with ordered chapters instead of silently skipping missing audio; fixed stale citation-reader reopening and playback notices. A consistent read-only copy of the owner's two-notebook SQLite library plus its referenced originals/final WAVs supported muted playback and a native WAV download that matched byte-for-byte; original library and credentials were unchanged. A partial-settings probe caught Zod defaults resetting omitted fields, corrected by merging before full validation. Recording media now verifies the original SHA-256, and bundle restore validates all staged originals/audio before publication. Same-size corruption and malformed-restore probes passed. All 35 existing application tests pass; the UI detector returned no findings. Version is bumped to 0.2.7 and packaging is in progress. Await deletion preference and review shared-file ownership before implementing cleanup.

The owner requested broad bug finding/fixing, removal of the native menu bar and a settings return button at the sidebar bottom. They then authorized continued work until told to stop, including coding/research contributors. No real app generation is permitted. The owner confirmed that the unwanted bar is `SenniBook / Edit / View`; preserve the native window controls and in-app library/status strip.

Initial fixes and recorded Electron interactions are in [the current audit](docs/audits/2026-09-14-no-generation.md). Numeric goals had been losing leading numbers; short valid source files were rejected; a later background-upload response could display the wrong notebook; creation errors were behind the modal. Fixed those paths, added return-to-previous-section sidebar navigation, guarded rapid actions, improved long-title/sidebar sizing, and made provider saves merge only the selected fields. All 35 existing application checks passed after the initial fixes. Isolated Electron imports, draft recovery, backup/restore and three-width settings inspection passed. No user library/credential changes, provider calls or new automated test files. Luna backend/playback reviews and further verification are in progress; desktop packaging is pending. Preserve other threads' pre-existing source/research changes.

## 2026-09-14 — focused learning research and candidate interview

The owner said “start,” authorizing the substantive research and conversation-prototype stage. Used the Deep Research and Matt Pocock research workflows, including a background primary-source research agent; used the PDF skill to inspect original papers. Active effort across parallel work was not measured, and no duration claim is made. Earlier hour estimates remain planning history.

Created [findings and decision memo](docs/research/learning-experience-findings.md), [candidate learner conversation](docs/research/learner-interview.md), and [learning-study evidence memo](docs/research/learning-evidence.md). Inspected original clarification studies, ten full-text learning-study publications, the perceived/actual-learning physics study, and the N.A.G./grilling references. Limited-access leads are explicitly separated. The recommendation preserves five required areas, follows up on consequential ambiguity, and confirms a concrete brief; no reviewed evidence establishes five as an optimal count or a quantified learning gain.

Compared four conversation alternatives and inspected fourteen constructed cases covering annotated reading, requirements-based history, economics method selection, ambiguous and contradictory answers, missing evidence, source/commentary language, duration conflicts, returning briefs, and walking without visuals. These are analytical inspections, not participant or provider trials. Counterevidence tempered claims about frequent quizzing, self-explanation, and walking limitations. A separate draft review found and corrected narrowed German reading scope, insufficient method-selection assessment, and repeated knowledge/assistance questions; a final study-claim check found no additional material mismatch with the evidence memo.

Updated PRODUCT.md, ROADMAP.md, and the plan's continuation record. No production prompts, application files, or automated tests were changed. Research prototypes are ready for rehearsal. Still outstanding: actual course-pack inspection, learner observations, model/episode comparisons, listening and delayed learning tasks, collaborative subject prompts, and implementation. Next useful evidence requires a real economics exercise with available answer guidance and, for source fidelity, the actual text selected for annotated reading. Biology remains open for its intended learners. Preserve the independent technical work below.

## 2026-09-14 — outcome-based effort and accepted question areas

The owner clarified that ten hours was an illustrative number, not a required research duration, minimum, maximum, or deadline. They expect the agent to interpret intent, estimate effort using the actual work and evidence, and push back on arbitrary constraints. Earlier fixed-hour framing was an assistant error. Updated AGENTS.md, ROADMAP.md, and the research plan to remove that requirement. Estimates are provisional and readiness depends on outcomes and evidence.

Inspected official GOV.UK research-planning and moderated-usability guidance plus the IES learning-guide recommendations page for scoping. Recorded initial estimates for a focused review, candidate conversation, and initial evaluation, with assumptions and clear separation from implementation or demonstrated learning effectiveness. This was a scoping check, not the completed academic review. The owner also endorsed the five starting areas from the preceding response; recorded that endorsement in PRODUCT.md and the research plan. No application behavior or production prompts changed.

## 2026-09-14 — five core questions and source treatment

The owner clarified the questioning proposal: about five required standard questions, with targeted follow-ups until consequential ambiguity is resolved. The assistant's earlier framing of a compulsory long interview overstated the request. Required preparation should elicit needs the learner might forget or omit in a rush; a generic overview request should not silently default to summarisation.

Recorded two concrete historical examples in the research plan: German literature needed an annotated reading preserving original wording and requested material; a Roman-Empire history chapter needed explanation of specified terms and ideas from a requirements document. Coverage, fidelity to wording, and the learning task are separate decisions. Updated PRODUCT.md and ROADMAP.md accordingly. No prompt or application behavior changed; the original course documents remain unavailable and substantive research remains outstanding. The earlier fixed-hour interpretation was subsequently corrected in the entry above.

## 2026-09-14 — learning experience and collaboration direction

The owner established this discussion thread for learning experience and prompting rather than architecture or implementation. Economics needs include selecting and performing calculations from questions, even when the underlying concept is understood. The owner proposed a research-oriented interview before audio generation and accepts substantial preparation effort for lasting value. They subsequently clarified that proposals, including mandatory long questioning, must be challenged rather than treated as unquestionable decisions; explicit final overrides settle disagreements.

Created AGENTS.md as the durable quality/collaboration agreement and CLAUDE.md as a pointer to it. Created docs/research/learning-experience-plan.md with confirmed needs, competing hypotheses, separate review passes, and continuation criteria. Its initial ten-hour allocation was an assistant overinterpretation, subsequently removed following the owner's correction above. Updated PRODUCT.md and ROADMAP.md to remove the assumption that minimal preparation is the overriding goal and to distinguish the proposed experience from current app behavior. No production prompts or application behavior changed. Document preparation is not completed learning research. Additional automated test writing remains deferred under the existing owner instruction.

Next for this workstream: develop the needs map and failure cases, then examine primary learning research and alternative questioning approaches. Other technical milestones below retain their own status. Preserve this thread's discussion/research scope when continuing it.

## 2026-09-14 — desktop development begins

Baseline: local React/Vite + Express/SQLite app, six passing tests, verified Codex connection and outline generation. Google speech is implemented but has not been exercised with an activated Cloud project. Existing data stays intact.

Current work: Electron packaging and lifecycle, efficient audio exports, source/audio import and reliability review. OpenCode CLI exposes `opencode-go/muse-spark-1.3-contributor` and has a configured OpenCode Go credential; execution still needs verification.

Read ROADMAP.md when resuming. Record tested artifacts, remaining failures and next work here, rather than treating an intermediate milestone as completion of the owner's broad request.

Owner steering: defer all further automated test writing until explicit wrap-up. Existing test files are retained. Continue building, manual checking and milestone build checks. Answers received: English default, iPhone listening, no Google Cloud project yet—include guided setup.

Milestone progress: Electron 44.3.0 desktop shell, installer and portable executable built. Packaged executable startup verified with its own runtime, Codex/OpenCode discovery and MP3 availability. Private `sennibook://app` origin now persists local playback storage across backend-port changes. Sandboxed renderer, narrow IPC, tray continuation, sleep prevention during generation and graceful shutdown are implemented.

Integrated source/goal snapshot Markdown export, source hashes, advisory script checks, OpenCode Go CLI authentication, Google project preference + guided setup, resume player, bounded WAV/MP3 export, fingerprinted atomic audio caches, and streaming ZIP notebook backup/restore. Manual desktop round-trip restored a notebook to a separate copy with its sources. Real OpenCode Go request through the app adapter succeeded. No real Google speech yet (no project/ADC). Installer artifacts need a rebuild after the latest backup UI and subsequent changes.

Owner does not currently use Obsidian; keep the standalone library first and vault integration optional. Current work continues into generation activity/recovery and listening polish. Tests are intentionally not being expanded; the pre-steering desktop smoke test still assumes an HTTP page URL and needs adaptation during the final sweep. Full-page screenshots of the hidden packaged window timed out; viewport capture worked in the source desktop runtime.

Real desktop OpenCode Go check: a five-minute illustrative notebook produced one chapter covering all three supplied goals and a 724-word, nine-turn script. Activity history recorded completion; no renderer errors. Screenshot: ignored test-results/generation-history-desktop.png. No paid speech generated. CLI cancellation now stops the Windows process tree and waits for close; OpenCode output groups retry messages instead of concatenating them. Episode copies permit editing after original audio is locked. MP3 downloads carry descriptive filenames and title/artist/album metadata.

## 2026-09-14 — source and audio import milestone

Source import now accepts TXT, MD, `.markdown`, CSV, PDF, DOCX, HTML and PPTX. Document extraction remains bounded, preserves PDF page and PPTX slide markers, and reports structural extraction warnings. Original source binaries are stored by SHA-256 under `data/originals/`; audio recordings are retained there as well, with an import limit of 500 MB. The source reader supports playback of original media and timestamp navigation from transcript segments back to the recording. Verification confirmed the 802,250-byte original download matched its stored hash, a request for bytes 0–99 returned HTTP 206 with 100 bytes, and a transcript timestamp click sought playback to 9.64 seconds with no renderer errors.

Portable ZIP v2 backups include the notebook manifest, immutable episode snapshots, referenced generated audio/cache files, and original source/media bytes. Restore remaps IDs and cache paths into a new notebook without overwriting the existing library. Legacy ZIP v1 backups remain restorable, including backups that contain extracted text but no original bytes. An original plus its transcript completed a successful backup/restore round-trip.

Local transcription uses faster-whisper with `large-v3` or `large-v3-turbo`. Python is required; guided setup creates a managed isolated Windows virtual environment, and each recording supports automatic language detection, English, or Dutch. GPU support is opt-in through additional Windows CUDA/cuBLAS/cuDNN packages in that environment. Real isolated CPU and CUDA validations passed with the same 18-second synthetic English clip, each producing four segments and 38 word timestamps; the CUDA run used the RTX3060 with `device=cuda` and `float16`. These validate the integration path only and are not quality benchmarks; Dutch quality validation has not been claimed.

OpenCode Go Spark contributed a passage-selector proposal at ignored .work/agents/spark-passages.json. It requires review/correction (overescaped regex/newline literals and weak goal diversity) before integration; current 240k source cap still applies. Larger-source retrieval remains future work. Installer artifacts predate this source-import milestone and need rebuilding after integration.

Source/recording milestone packaged: Windows installer + portable 0.2.0 rebuilt. Packaged worker status found prepared Python/model/CUDA. Real packaged OpenCode chat returned two exact recording quotes, preserved the qualification 'alone', and a citation opened original audio at 9.6 seconds without autoplay or renderer errors. Corrections retain original machine text and segment timing, clear obsolete word alignment and coverage, reject stale edits (409), and survive ZIP restore (201). Mobile document width check had no horizontal overflow; hidden packaged screenshot capture timed out, so do not claim visual mobile verification. New tests remain deferred. Next active work: bounded source passage selection and explicit consulted-context disclosure for larger course packs.

## 2026-09-14 — larger source packs and saved source readers

Replaced the former whole-source cap with bounded, deterministic English/Dutch lexical passage selection (90k serialized source context for cloud providers, 24k for Ollama). Coverage runs in groups of up to 12 goals. Chat, outlines and chapter scripts retain the supplied source ranges, with a collapsed disclosure in the interface. Quotes are validated against the actual supplied ranges; adjacent ranges are merged for validation. Unmatched terms and missing evidence in a selected subset are disclosed without claiming the whole source lacks the answer. This is lexical selection, not semantic retrieval or a completeness guarantee.

A real large-request check exposed OpenCode's attached-file reader omitting later material. The adapter now sends the complete request through standard input; a real 89,266-character request and a subsequent app chat/coverage check passed. The app selected 1,800 characters from a 279,235-character illustrative archive and returned five verified chat quotes retaining the late passage's qualifications. See [request-delivery evidence](docs/research/opencode-request-input.md). Title matches alone no longer fill the request with unrelated passages.

Manual Chromium checks at desktop and 390px width had no renderer errors or horizontal mobile overflow; screenshots were inspected. Passage links use their stored offsets, including repeated text that otherwise matched an earlier occurrence. Context metadata survived ZIP restore. Episode readers now support original downloads, document excerpt highlighting and read-only timestamped audio from the saved source version. Current build:desktop passes; real plan/script and snapshot-original checks are underway. Release artifacts still predate this cohort. No new automated tests were written.

Primary-source local OCR research is recorded in [local-ocr.md](docs/research/local-ocr.md). OCR implementation has not yet been added. The next technical question is whether the existing PDF.js Node canvas path packages cleanly, before adding scan/image recognition.

Follow-up real Go validation completed in a new isolated illustrative notebook: outline HTTP 200, asynchronous script HTTP 202 then 10 turns, with both sources retained in plan/chapter context. Removing the current audio source left the episode's original downloadable (200, matching 802,250-byte SHA-256) and seekable (206, 100-byte range). The older existing automated suite was run without changes: 25 checks passed; integration's mock no longer recognizes the revised coverage-prompt wording, and jobs cleanup fails on Windows because its SQLite connection remains open. Both test-harness updates are deferred to the owner's final sweep. These are not reported as a green full suite.

Owner requested a usable preview now. Launched the existing packaged portable 0.2.0 with its normal application-data library; verified a responding SenniBook window and backend ready log. The command environment's ELECTRON_RUN_AS_NODE had to be removed for launching the desktop GUI; ordinary double-click launch is unaffected. The running user preview must not be shut down or used as validation data. Owner confirmed scans/photos are mostly printed pages and slides. Continue development separately, with the next build carrying OCR and the newer context fixes.

Owner requested a preventive review of a similar project's UI issues and upstream solutions, prioritizing behavior over appearance. Started a bounded Luna contributor on Claude Code's official public issue tracker and asked whether the intended reference was Claude Code, OpenCode or both. Findings belong in docs/research/ui-reference-review.md and must be reviewed against this app before implementation. No cosmetic redesign or automated test writing is implied.

Owner immediately clarified the intended reference is **T3 Code**. Redirected the contributor accordingly; Claude Code and OpenCode are not the requested UI reference.

## 2026-09-14 — printed sources and desktop 0.2.1

Implemented local printed-page OCR with Tesseract.js 7 and bundled English/Dutch models, original retention, page markers, cancellation, retry and bounded per-page caches. PDFs prefer existing text layers unless the learner requests all-page recognition. PNG/JPEG originals have an expandable image preview. Fixed pending scans being mistaken for active jobs, cancellation availability, a long Windows cache path, and fast jobs finishing between UI polls. Existing extracted text is kept until successful replacement. Large iPhone-sized photos are bounded before recognition.

Packaged PDF import initially failed because PDF.js treated Electron's utility process differently from ordinary Node. Explicit local worker and native canvas configuration fixed both extraction and raster OCR. The rebuilt 0.2.1 executable imported an illustrative scanned PDF (zero initial text), recognized its English/Dutch lines with a page marker, imported a selectable biology PDF, and recognized it in all-page mode. Both sources, OCR page metadata and original hashes survived a ZIP round-trip into a new notebook. Isolated notebook: 641b1064-fc33-4e5f-a2b3-3bb97ad20699; restored copy: 11b35819-0471-491b-88e3-d8cb02c58c7a under .work/packaged-021/data. No renderer errors. Earlier packaged PNG and saved audio snapshot checks also passed. These are integration checks on authored material, not OCR accuracy benchmarks.

The user's startup-error screenshot pointed at .work/pdf-utility-app/desktop.log. That was an agent diagnostic launched with electron/main.cjs as the app path, causing a duplicated electron/electron/backend.cjs lookup. Corrected the diagnostic to launch the project root; explicitly hidden diagnostic instances now log startup errors without opening user-facing error boxes. Normal launch error reporting remains. The normal 0.2.0 user preview was left running and its library was not used for validation. Offered the verified portable 0.2.1 executable for the owner to open when ready.

T3 Code's issue review is recorded in docs/research/ui-reference-review.md. Root reproduced unsent chat text disappearing on tab navigation in the previous package. Bounded deferred draft persistence is being integrated into source, goal, chat and script editors; this work is not in the verified 0.2.1 artifact yet. Script edits now report specific turn-count and character-limit errors before sending, preserving the typed text. No new automated test files have been written. Next: manually exercise draft navigation, reload, failure and late-request ownership before packaging that cohort; preserve the distinction between technical reliability and learning quality.

## 2026-09-14 — draft preservation and navigation

Integrated bounded, deferred draft storage for chat, source title/text/type, goal list/type and individual chapter scripts. Navigation/reload restores unfinished work. Accepted requests only clear the submitted version; newer text survives a delayed response, and accepted old text is removed from an unchanged remounted editor. Root review caught and corrected acceptance before debounced storage catches up, plus an unchanged remount re-persisting already accepted text. Each script draft stays with its notebook/episode/chapter. Closing keeps a draft; discarding script edits is explicit. Script baseline formatting is memoized rather than repeated for every keystroke.

Manual Chromium checks passed for tab/reload retention, simulated provider failure, late responses after remount, newer typing, acceptance before the debounce, chapter switching, long-turn rejection, actual source/goal/script saves, and preservation of another chapter's unsaved edits. Quota failure retains the current input and shows a warning. Unavailable local storage no longer blocks library opening. These simulations are separate from live provider checks. No renderer errors were observed in the successful runs. Several initial check selectors needed correction for exact tab names, restored textarea labels and Dutch number separators; these are not reported as application failures.

The navigation check exposed the old mutation-busy gate ignoring a second notebook selection during loading. Navigation now has separate pending state and a request sequence so a slower old response cannot replace the latest selection. The delayed-response check passed. Explicit field label references prevent restored textarea contents becoming part of their accessible label. Build:desktop passed at version 0.2.2; package verification and an independent targeted review are underway. No new automated test files were written.

The independent review found two additional cases: a draft accepted/discarded in one browser tab could delete a newer draft saved in another, and notebook deletion could redirect a later notebook selection. Guarded draft storage changes are underway. Deletion now preserves a different selected notebook; a delayed real DELETE of an isolated notebook passed that check. The 0.2.2 packaging candidate completed a real OpenCode Go chat through Electron, with three exact verified quotations retaining the coalition example's qualification, complete two-source context metadata, and no renderer errors (isolated notebook fa95dac1-9803-43d8-9455-f891463b5f86 under .work/packaged-022/data). Final candidate recommendation waits for the draft review corrections.

Electron-builder's downloaded-runtime extraction hit a Windows EPERM on its temporary-directory rename twice, including in a fresh output directory. Packaging succeeded using the installed matching Electron 44.3.0 distribution: `npx electron-builder --win --config.directories.output=release/0.2.2 --config.electronDist=node_modules/electron/dist`. No running user app was stopped. The failure's specific Windows lock holder was not established; do not attribute it to antivirus or an app process without evidence.

The draft corrections passed root two-page checks for foreign-newer-record preservation during autosave, acceptance, empty cleanup and discard, including reload of the newer draft. Subsequent typing after a conflict does not silently adopt and overwrite the foreign version. Storage mutations compare the last observed record; this is not a fully atomic synchronization primitive. Root also guarded invalid-record cleanup, clarified conflict recovery copy, and removed an inaccurate claim that failed-to-restore data was necessarily discarded. The independent contributor verified fast acceptance before debounce after adding these guards.

A 390px screenshot exposed the editor's first action button clipped off the left edge despite document width equalling viewport width. Added action wrapping; all three button bounds now fit the viewport and the corrected screenshot was inspected. Full build:desktop passed after the review corrections. Final installer/portable rebuild is running; next check packaged startup, OCR and draft retention across an application restart before recommending 0.2.2.

Final 0.2.2 artifacts are built in release/0.2.2. The actual portable executable started, reported version 0.2.2 and the isolated .work/portable-022-final/data path, recognized the English/Dutch printed PDF, and retained the question draft after its processes stopped and a new portable process opened the same library. Notebook d6edfe44-ece0-4d6c-98cf-2c364c907aa5 contains only authored validation material. Direct Playwright Electron attachment to the NSIS portable wrapper timed out even though the app had started; connecting through its local debug port worked. One probe connected before the first page existed and was corrected to await the page. The first CDP target close left a diagnostic Electron main process behind; its exact verified temporary executable/PID was stopped, leaving the normal user app untouched. Restart assertions passed; the final CDP Browser.close completed process shutdown but left the probe's top-level close await unsettled. These cleanup/attachment issues are recorded separately from the successful application assertions.

No diagnostic SenniBook process remains. The normal user preview is still 0.2.0; the new 0.2.2 executable is ready for the owner to open after quitting it. This is an interim usable release, not project completion. Next: return to the core learning/audio work—guided Google setup and real speech validation when available, the bounded local-TTS experiment, actual course-pack evaluation, and the learning-experience workstream in its research plan. Keep first use simple. Additional automated test writing remains deferred to explicit wrap-up.

Local preview checkpoint: 4a9cb1a. Concurrent work subsequently appeared in server/cartesia.ts, shared/speech.ts, the Cartesia/SpeechSettings components and src/main.tsx. Those newer files/edits are left intact and outside this checkpoint and the verified 0.2.2 executable; coordinate with that workstream before changing the speech controls. The isolated draft-development server remains on port 4412 with DATA_DIR=.work/draft-preview/data. No remote repository was created or pushed.

## 2026-09-14 — diagnostic startup path correction

The owner's screenshot names `.work/pdf-utility-app/desktop.log`. That log records the earlier diagnostic launch looking for `electron/electron/backend.cjs`; the normal app log instead records successful startup. Changed `electron/main.cjs` to resolve bundled assets relative to its own directory, which works for both a direct source-entry launch and the normal project/package entry. Root manually launched both entry forms in separate hidden, disposable libraries and verified the renderer reached the notebook screen and reported the expected isolated data path. No automated test files were added. The direct file entry reports Electron's version, so the project-root launch remains the supported development command. The existing verified portable 0.2.2 remains available; this source correction will be included by the next packaging run. Normal user processes and notebook data were left untouched. Concurrent Cartesia work remains outside this workstream.

## 2026-09-14 — Cartesia speech integration, version 0.2.3

The owner explicitly requested adding Cartesia to the site after obtaining a Pro subscription/API key. Implemented Sonic 3.6 (Cartesia API 2026-08-14) as an additional speech provider. Connections & settings accepts, checks, replaces and disconnects a host-only key; status and notebook exports never include it. The local credential file and temporary files are Git-ignored and explicitly denied by the Vite development server. Existing Google settings and fingerprints remain compatible; legacy notebook/episode settings receive defaults on read.

Audio studio now supports provider selection, searchable/paginated English/Dutch voice lists, two distinct hosts, delivery speed and character-based credit estimates. The estimate does not query or cap the account balance. Existing scripts can change speech settings without being rewritten; audio-bearing episodes are copied before changes. Unsaved voice changes disable preview/generation until saved. Missing voices do not block script planning. Each Cartesia request belongs to one host, and previews include the first segment from each host even when the opening turn spans multiple segments. Completed segments remain reusable through previews, retry and cancellation. WAV/MP3 exports use the existing pipeline.

Verification: build:desktop passed; 20 existing core, audio-export, playback and quality tests passed (no new automated tests written). An isolated manual provider simulator exercised API-key connection and invalid replacement, A/B request mapping, preview playback, a 429 after partial generation, retry of only the failed segment, complete four-segment playback and MP3 download. Voice changes preserved original audio in a separate episode; ZIP export contained neither credentials nor the dummy key. Additional direct manual checks confirmed a long-turn preview includes both hosts, remaining segments are reused, cancellation resumes from cached work, and credential replacement/disconnect work. Four development-server URL variants (direct, raw query, @fs and encoded filename) returned 403 without the credential. Browser controls exposed the correct missing/duplicate voice errors and prevented generation with unsaved changes. Layout bounds fit the viewport at 390 CSS pixels; screenshot capture repeatedly failed in the preview tool, so no screenshot/visual-quality sign-off is claimed. Impeccable's mechanical scan returned no findings.

Built installer and portable 0.2.3 under release/0.2.3 using the installed Electron distribution. The unpacked packaged executable launched in .work/cartesia-packaged-023, reported its backend ready and remained responsive. The current real browser edition is running at http://127.0.0.1:4317 (PID 24296), with the preview opened to Cartesia settings. Validation used separate libraries; the owner's running desktop instance/library and the other thread's port-4412 draft preview were left alone. Existing desktop users must quit the old instance before opening the 0.2.3 executable. No real Cartesia key was supplied and no real Cartesia speech was generated: authentication against the owner's account, Dutch/English pronunciation and long-form listening remain for their first preview. Preserve the concurrent desktop startup-path correction and local-TTS research edits.

## 2026-09-14 — Cartesia sound-quality comparison

Owner reports studio-like website audio versus telephone-like SenniBook audio with the same voices but different scripts. Read-only inspection confirmed the completed plant episode uses Sonic 3.6 at speed 1.0, 12 complete turns/requests, and 24 kHz PCM16 WAV. Final PCM exactly matches the cached provider samples; joins introduce no lossy encoding or altered samples. Generated two short Gemma samples with identical text/model/voice/speed and 24 versus 44.1 kHz output (20.96/20.80 seconds) for owner comparison. App behavior and existing episodes remain unchanged; no causal or perceptual improvement is claimed before listening. See docs/research/cartesia-audio-quality.md. If both sound inferior, the next evidence needed is the website output/settings with the same text.

## 2026-09-14 — oversized outline repair, Electron 0.2.5

The owner reported two planning failures exposing a Zod too_big error for chapters[0].summary exceeding 3,000 characters. The prompt had not stated the schema limits and the planner immediately rejected any invalid output. Added explicit title/chapter/summary limits and a concise planning-description target, while explaining that complete goal text is supplied separately to script generation. Invalid JSON, schema failures and missing goals now allow one automatic repair using the original source/goal request and the previous output when bounded. Revalidate every goal after repair; never truncate source material or silently save an incomplete plan. Provider/auth/rate-limit errors and cancellation do not trigger repair. Failed repair reports a plain-language field error. The progress timer now retains provider/repair stages. Codex client version follows package metadata.

The earlier explicit wrap-up request lifted the additional-test-writing deferral. Added five focused regressions: exact 3,001-character failure and recovery with 38 goals/snapshots preserved; bounded failed repair/readable error; malformed JSON followed by missing-goal rejection; valid 3,000-character boundary with one call; provider failure/cancellation without retry. All 31 application checks pass, and build:desktop passes. A real Codex request against a read-only copy of the owner's gs notebook succeeded on the first attempt: one chapter, 2,202 summary characters, all 38 goals assigned. Output is in ignored .work/planning-fix/verified-outline.json. User library content and active work were not changed; no audio generated. Five minutes versus 38 goals remains a learning-depth limitation, not resolved by structural validation. Installer/portable packaging completed under release/0.2.5; packaged backend matches server-dist byte-for-byte. The packaged desktop smoke check initially timed out capturing a diagnostic screenshot of a hidden window. Made that nonessential image opt-in via SENNIBOOK_TEST_SCREENSHOT=1; the mandatory sandbox, private-backend access, notebook creation, persistence and clean-restart assertions then all passed (one packaged desktop check, 32 total with the application suite). The user's running app was not restarted; handoff requires quitting the previous version and opening the 0.2.5 portable.

## 2026-09-14 — Electron 0.2.4 repair and handoff

Corrected the desktop/browser mix-up: deliver in Electron and its application-data library. The old sidebar hardcoded 0.2 while the executable was 0.2.3; version now comes from package metadata. Preserved the existing gs notebook and both working speech connections. Consistently backed up the desktop SQLite database, then inserted the labelled houseplant demo with three linked original source notes, three goals, a real Codex outline and a 647-word script. It selects the user's two Cartesia voices. Full audio is left for the user to generate.

Codex now uses the official App Server integration, as T3 Code does, with SenniBook's own identity and existing login/account limits. The earlier categorical API-key recommendation was too broad; no absolute account-permission or ban guarantee is claimed. OpenCode Go separately describes coding-agent traffic, so lesson permission remains unconfirmed. Lesson threads disable tools and configured MCP servers without altering user configuration. Native hidden Windows Codex launch avoids the npm wrapper's visible grandchild console. Planning records persistent progress, elapsed time and failures, with an in-app connection check and reachable errors.

Real Cartesia returned streaming WAV with unknown-length RIFF/data headers, which the old parser wrongly rejected as incomplete. Finalize those sentinel lengths after receiving the whole HTTP body while retaining normal truncation/PCM checks. Real Google speech also succeeded. An isolated packaged run completed and played a short two-host Cartesia preview and verified Codex login. A native App Server run generated the demo outline/script. No full lesson listening or learning-effectiveness claim is made.

Validation: build:desktop passed; all 26 existing app checks and the desktop restart/persistence check passed. Corrected three existing harness issues: coverage fixture wording, SQLite close before Windows cleanup, and private desktop URL checking. No new tests. Final packaged backend matches server-dist byte-for-byte. Installer and portable are under release/0.2.4. Successful packaging after Windows archive rename failures: npx electron-builder --win --config.electronDist=node_modules/electron/dist --config.directories.output=release/0.2.4. No SenniBook window remained at handoff. Automatic approval review rejected the attempted launch command with only 'blocked by policy'; the final new executable has not been opened in the user's normal library.

Owner requested immediate wrap-up and spending-control guidance. Current Google docs: alerts-only budgets do not stop usage. New spend cap budgets cover Gemini API, Agent Platform/Vertex AI, Cloud Run and Cloud Run functions, not Cloud Text-to-Speech; supported caps also have latency. TTS quotas constrain rate, not monthly euros. Budget-triggered billing shutdown is delayed and affects the whole project. No Cloud budget, quota or billing setting was changed; the app has no enforced spending budget. Sources: https://docs.cloud.google.com/billing/docs/how-to/budgets-spend-caps ; https://docs.cloud.google.com/billing/docs/how-to/budgets ; https://docs.cloud.google.com/billing/docs/how-to/disable-billing-with-notifications ; https://docs.cloud.google.com/text-to-speech/quotas ; https://learn.chatgpt.com/docs/app-server ; https://github.com/pingdotgg/t3code/blob/main/apps/server/src/provider/Layers/CodexProvider.ts ; https://opencode.ai/docs/go/#where-can-i-use-it .

## 2026-09-14 — local voice experiment, first measured results

The owner confirmed that another agent is implementing cloud TTS credentials. This workstream remains isolated from those provider/credential files. Chatterbox Multilingual V3 installed in a separate Python 3.11 CUDA 12.4 environment and generated four authored English/Dutch passages on the RTX 3060. The speech checkpoint was pinned and all selected files verified; a truncated download was resumed and passed its complete hash before loading. Model load took 18.22 seconds after imports, short warm generation ratios were 1.54–1.82 seconds of work per second of audio, and peak PyTorch allocation was 3.30 GiB. No hour-long rendering or listening quality is claimed. One default voice is working; two stable hosts are not established.

Existing isolated faster-whisper large-v3-turbo transcribed the samples. Two coalition passages preserved wording; the numeric and biology passages have possible word/pH discrepancies requiring listening, because ASR can itself be wrong. Initial PCM conversion exposed a few clipped samples, so the longer boundary experiment retains raw float waveforms and reduces peaks only when making PCM16. The pinned generator has a 1,000-output-token cap, so the cloud chunk size is not suitable unchanged. The first model load also downloaded an otherwise unused Chinese tokenizer into its upstream default cache despite Hugging Face offline mode; subsequent preparation explicitly controls that cache. No ASR dependencies or models were changed and no automated test files were written. Findings and exact revisions are in docs/research/local-tts-v3-experiment.md; local inputs, metrics, package resolution and WAVs are in .work/local-tts-v3. Longer boundary calls and their transcript checks are in progress; this is not a production adapter.

The two longer local-TTS inputs (168 English / 172 Dutch words) produced valid WAVs but failed transcript comparison badly. The Dutch generation reached the 1,000-token cap; the English generation ended earlier and still lost substantial content. This rules out reusing the existing cloud chunk size without additional controls. The next isolated comparison preserves each input exactly while splitting into five complete-sentence groups per language, each at most 45 words; generation/ASR comparison remains in progress. Ordinary Python network attempts were rejected after imports in the longer run, with explicitly prepared caches. Separately, inspected the other agent's packaged 0.2.3 main entry: it matches the current source byte-for-byte and includes the diagnostic startup-root correction (SHA-256 931b660010f4c34a9e833ae8ac2ddf53c640709cbebb4d84879421f87b3129af).

The exact-text recovery experiment completed all ten sentence groups. Joined English/Dutch outputs were 55.88/57.04 seconds, generated in 79.17/82.61 seconds after loading. ASR recovered both passage sequences and closing qualifications, while still flagging possible word errors. Appended recognition fragments have implausible 0.12/0.06-second durations and low word confidence, so ASR artifacts are a strong alternative explanation; do not attribute those fragments to the voice without listening. Peak PyTorch allocation across the recovery was 3.35 GiB, reservation 3.91 GiB. The environment/model/cache/checkout logical size is about 8.75 GB, excluding shared uv caches. Full findings and limitations are recorded in the experiment report. A checked seven-entry listening ZIP (six WAVs plus authored-text README) is available at release/SenniBook-local-voice-comparison-2026-09-14.zip; it contains no model weights or credentials.

Assigned the existing Luna research contributor a bounded maintained-Piper CPU control in its own .work/local-tts-piper environment: two fixed English presets and two Dutch presets, two authored passages per voice, exact revision/file checks, timings and optional existing-ASR diagnostics. Contributor owns only a new docs/research/local-tts-piper-experiment.md and ignored local artifacts. Do not overwrite the cloud provider/credential work. Chatterbox generation and ASR processes have completed; no production local speech provider or automated tests were added. Next: inspect the Piper evidence and arrange actual listening feedback before selecting a local two-host path.

## 2026-09-14 — native Cartesia audio quality, Electron 0.2.6

The owner confirmed that sample B (44.1 kHz) sounds right and authorized making it the default. New notebook/planning settings now default to native 44.1 kHz PCM16 WAV. Audio quality offers High (44.1 kHz) and Standard (24 kHz) in the existing voice controls. Cartesia requests and streaming-WAV finalization both honor the selected rate; there is no upsampling or post-processing. Legacy episode snapshots receive 24 kHz on load and ZIP import. Their original cache fingerprint remains byte-for-byte compatible; 44.1 kHz has a distinct fingerprint so it cannot reuse 24 kHz segments.

Changing quality on an audio-bearing episode uses the existing separate-copy flow, preserving its script and original recording. A focused four-test regression file checks defaults/legacy cache identity, both WAV formats and truncation, actual request-body rate/voice mapping, revision generation and backup/restore. All 35 application tests and build:desktop passed. Packaged desktop startup, sandbox, private backend, notebook persistence and clean restart also passed. The packaged backend matches server-dist byte-for-byte.

An isolated packaged UI run loaded a legacy 24 kHz episode, selected High, saved a distinct episode copy and generated a real two-host Cartesia preview: 8.4 seconds at 44.1 kHz, with no renderer errors. The original WAV and copied script were unchanged. Only two short authored sentences were synthesized; no full user episode or normal desktop library was changed. Hidden-window screenshot capture timed out at the five-second diagnostic limit; no screenshot sign-off is claimed. Verification metadata and the temporary script are ignored under .work/cartesia-quality. Owner preference and causal limits are in docs/research/cartesia-audio-quality.md.

Installer and portable 0.2.6 are ready under release/0.2.6, built using the installed matching Electron distribution. README identifies the latest executable and explains how to upgrade existing audio. The owner should quit an older instance and open SenniBook-0.2.6-portable.exe. No commit or push was made. Preserve the separate local-TTS research work.
