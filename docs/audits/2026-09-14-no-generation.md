# Desktop reliability sweep — 2026-09-14

Status: ongoing. The owner requested continued exploration and fixes until asked to stop. No app generation: later permission to use coding/research contributors does not lift that restriction.

## Scope and evidence

Isolated Electron libraries live under ignored `.work/app-audit/`. The owner's library and credentials are not fixtures. No model, speech, OCR or transcription generation was requested. Existing automated checks use local doubles/fixtures. No automated test files were added. Temporary interaction scripts/screenshots are diagnostic records, not educational evaluation.

The owner confirmed the unwanted second bar is the native `SenniBook / Edit / View` menu. Preserve the Windows title bar and in-app library/status strip. Hide the native menu by default; Alt reveals it and accelerators remain available.

## Shipped in local desktop 0.2.7

- **P1 — numeric goals altered on paste.** Actual Electron interaction saved `2.5% inflation` as `% inflation`, removed the year from `1914 and the start of war`, and removed the temperature from `-5 degrees Celsius`. Strip only bullet/numbered markers followed by whitespace. Repeat interaction preserved all three while stripping normal list markers. Already altered goals cannot be reconstructed automatically.
- **P1 — background import replaces selected notebook.** `change()` compared selection at request start/end without checking the returned notebook ID. A later upload could start after selection changed and replace the visible notebook. Match the response ID to selection. Delayed two-file import retained the other notebook and saved both files to the intended destination.
- **P2 — short valid sources rejected.** Extraction required 20 readable characters. Accept nonempty readable text; continue rejecting empty files. Electron imported a 19-character economic identity and retained its original. Printed-page OCR detection remains separate.
- **P2 — creation error hidden by modal.** Put save errors inside the named new-notebook dialog. A simulated 503 displayed an alert inside the open dialog and retained its input.
- **P2 — settings return route missing from sidebar.** Replace the settings control with `Go back` while settings is open. Return to the previous notebook section and its top. Confirmed return to Learning goals. Remove the old page-top return control.
- **P2 — desktop heading overflow.** Allow heading children to shrink and wrap. A 150-character unbroken notebook title no longer widened the document.
- **P2 — large-library sidebar sizing.** Let the notebook list shrink while fixed controls remain intact. With 47 notebooks, the back control remained visible at 700×520 and 1400×880 content sizes. Phone navigation uses the existing compact header; increase its new/back targets to 44px.
- **P2 — repeated actions bypass busy guard.** Add a synchronous busy ref. Cancellation no longer clears another operation's busy ownership before it completes.
- **P2 — provider save overwrites unrelated settings.** Submit only provider/model; merge before full schema validation. Zod's optional defaults initially reset omitted fields, which the focused interaction caught; merging raw object fields before validation corrected that. Verified preservation of Economics, Dutch, 35 minutes, starting knowledge and 44.1 kHz when saving only the provider/model.
- **P1 — preparation drafts disappear on navigation.** Reproduced lost listening preparation, model ID and notebook description on a settings round trip. Reuse draft recovery with per-field object patches for episode setup, provider/model, notebook details and episode voice edits. Empty strings are preserved; discard controls distinguish drafts from saved values. Save completion accepts only the exact submitted draft, protecting newer edits. Confirmed recovery of each form and episode voice selection.
- **P2 — playback navigation and speed reset.** Remember selected episode/chapter and playback speed on this device. An existing episode's position resumed correctly but its speed reset to 1×; the updated multi-episode interaction retained the selected third chapter and 1.5×.
- **P1 — unfinished chapters silently skipped.** Natural completion and hardware Next searched for any later playable chapter while the visible Next control used the next chapter in order. All three now follow chapter order. A hand-authored three-chapter diagnostic using copied existing WAV bytes stopped on the missing middle chapter both at natural completion and via Media Session Next. This is a navigation check, not generated lesson evidence.
- **P2 — closed source reader reopens.** Clear the originating source request when closing the reader. Opening a saved diagnostic citation, closing it and switching sections no longer reopened it.
- **P2 — stale recording-playback notice.** Clear autoplay/error notices after native Play succeeds.
- **P1 — same-size original-recording corruption accepted.** The recording endpoint checked file length but not saved SHA-256 identity. Use the shared original verifier, cancelling verification if the request disconnects. A four-byte diagnostic returned 200 before corruption and a readable 400 after same-size byte replacement. Hashing large recordings before serving can add latency; no claim of protection against a malicious local administrator or filesystem replacement race.
- **P1 — malformed restores publish orphan originals.** Validate every staged original hash/size and audio/cache payload, then remap/validate references, before publishing originals. The malformed-later-entry probe failed without writing originals. Commit-stage filesystem/persistence failures can still leave unreferenced files: blindly deleting content-addressed paths could damage a concurrent successful import. Shared-storage cleanup needs a separate ownership design.
- **P2 — saved mutation reported as failed when list refresh fails.** Preserve successful save semantics and show an explicit stale-list warning instead of encouraging a duplicate retry.

## Completed checks

- TypeScript, Vite and desktop-backend builds passed after the initial fixes.
- All 35 existing application checks passed after the complete second fix set.
- Isolated Electron: notebook creation, numeric goals, short/Unicode/HTML imports, original retention, source-draft recovery across sections, modal save failure, delayed multi-file navigation, long title, settings return route.
- ZIP export/restore preserved three sources, original references and six goal texts in a distinct notebook. Source IDs are scoped within each notebook and may be retained; this is not a collision. This interaction did not check episode/audio restore.
- Settings screenshots at 1400, 700 and 390 pixels showed no document horizontal overflow. Renderer error collection was empty for that workflow.
- Took a consistent read-only SQLite snapshot of the owner's two-notebook library and copied referenced originals/final WAVs into an isolated application-data directory. No credentials were copied. Existing audio loaded (240.08 seconds), played muted, sought correctly and resumed at its saved position. Native Electron chapter download completed and matched the source WAV byte-for-byte. The owner's data was not changed.
- Impeccable's mechanical detector returned no findings across the changed UI files. This is not a full accessibility certification; the deliberate light theme remains unchanged.

## Desktop 0.2.8 checkpoint and continuation

0.2.8 installer and portable are built under `release/0.2.8/`. All 35 existing application tests and packaged startup, private-backend access, sandbox, persistence and restart passed. Packaged UI workflows passed Trash/restore/purge, shared-file and activity/draft ownership, downloads, empty-file rejection and filter resets. The backend, Electron bridge and HTML match the checkpoint output; backend SHA-256 is `e2bb63e5294ddc93b7b018d63dba855369d5e7becf8ede435d4e6d0a996df69f`. The mechanical UI detector returned no findings. The earlier 0.2.7 checkpoint separately verified numeric goals, imports, drafts, ZIP restore and 47-notebook/three-width settings with no renderer errors.

The owner selected recoverable Trash with explicit permanent deletion. Shipped in 0.2.8: ordinary deletion retains the complete notebook in SQLite Trash, with files in place; restoration is transactional; permanent deletion checks references from active and trashed notebooks before removing unshared files. A process-level artifact lock protects import publication and cleanup. A partially failed purge stays visible for retry and cannot be restored after bytes have been removed. No automatic old-orphan cleanup or multiple-backend support is claimed.

Source-build Trash interaction passed: move retained all files; restore kept the same notebook ID and its draft, while retaining the other selected notebook; cancelling permanent deletion retained the entry. Confirmed purge removed unique current/historical originals, final audio and chapter cache, plus local draft/resume keys, and preserved a shared original referenced only by another notebook's historical episode snapshot. The desktop and 390px layouts had no horizontal overflow and 44px action buttons. Renderer errors were empty. A simulated list-refresh failure after a successful move exposed a stale deleted selection; that failure path now clears it and reports that the move succeeded. Backend preflight rejects an unknown cache file without deleting bytes, keeps the entry restorable, and records its error. Owned final/preview/temp/quarantined cache cleanup passed unshared/shared probes; activity cleanup passed the packaged interaction, and post-transition failure/retry passed an isolated backend fault probe.

The synthetic performance review found a 5 MiB source pack plus three full episode snapshots exceeds the 20 MiB uncompressed ZIP manifest ceiling. At 100 synthetic 1 MiB notebooks, the sidebar summary route's previous full-body parsing took roughly 232–238ms and allocated about 191 MiB of extra RSS in the diagnostic process. These are bounded synthetic measurements, not a production profile. The review is preserved in ignored `.work/performance-review/report.md`; summary querying and backup/download failure handling are the next incremental reliability work.

Additional draft stress checks against the checkpoint preserved an intentionally empty description, retained a newer unsaved model after a delayed earlier save, retained a 5,001-character preparation draft without clipping, and restored the saved setup when discarded. Double-clicking create in the same event cycle made one POST.

Source/goal filtering now has useful empty states, accessible selection/expansion states, and avoids repeated source word counting during status renders. The library summary route now reads a separate cached table maintained by SQLite triggers; it preserves the two-column notebook table and remains synchronized with legacy SQL writes and trash/restore. Migration/restart and failure rollback diagnostics passed. The write-time parsing tradeoff and synthetic measurements are preserved in [the size/performance audit](2026-09-14-notebook-size-performance.md).

Creation had the same saved-but-stale-list failure as other mutations: a successful create followed by a simulated sidebar refresh failure left the welcome screen empty. It now opens the returned saved notebook directly and reports the sidebar warning. ZIP restore shares that path. Multi-file import errors identify the failed file and how many files already saved, reducing duplicate retries. Source-build checks passed after these changes.

Failed downloads could navigate the desktop app into a JSON error response. The new narrow main-process download bridge keeps the private token outside the renderer, allowlists file routes, bounds/redacts errors, streams into owned temporary files and supports cancellation. Source-build UI checks passed for native save selection, Markdown, ZIP, exact original bytes, a backend 400 preserving both the current screen and an existing destination, and save-dialog cancellation. Desktop/390px panels fit and renderer errors were empty. A forced shutdown/crash may leave an uncommitted `.part` file; it is never presented as a completed export. Declared response lengths are checked before publication; a cleanly malformed success response from an arbitrary mock server is not by itself evidence of a reachable production truncation bug.

Zero-byte multipart file upload hangs in the Electron custom-protocol forwarding path even though the backend finishes. Direct HTTP returned 400 in about 23ms; zero-byte renderer upload timed out at eight seconds; positive-size whitespace and unsupported files returned normal 400 responses through both paths. Source and ZIP restore controls now reject empty files before sending them. A multi-file interaction saved the first file, named the empty second file, explained the partial import and skipped later files. Source-search and goal-filter reset actions passed. Filtering empty stream chunks did not fix the transport hang; replacing Electron fetch with Node fetch lost the useful error response. Both experiments were reverted, and the demonstrated UI preflight shipped in 0.2.8.

A separate compact snapshot codec is being developed for a later versioned backup format; it is not integrated or claimed to fix ZIP backup sizes yet. Root review corrected leaked wire-only episode references and moved count/aggregate-byte checks ahead of parsing large entry sets. ZIP integration and full schema round trips remain outstanding.

Keep implemented fixes distinct from product recommendations. Spending controls, durable generation checkpoints, and an explicit review of old unreferenced storage remain candidates; verify current behavior before relying on the older audit. Software checks do not establish semantic source verification or learning effectiveness.

## Post-0.2.8 checks in progress

Keyboard inspection found that the hidden mounted notebook-name input's autofocus did not run when its dialog opened, leaving focus on Close; focus is now explicitly applied after opening. The skip link now focuses a programmatically focusable main region. The interaction confirmed input focus, Escape restoring the launch button, and the skip link targeting MAIN before the next content control.

Source and backup selections preflight the server's existing 20 MiB document, 500 MiB recording and 2 GiB ZIP limits. A synthetic 21 MiB document produced a readable error before any upload request, retained the earlier imported source, and did not break partial-import reporting or source/goal filter resets. These changes are not part of the packaged 0.2.8 checkpoint.

An independent fault probe relayed real local original, bundle and episode-WAV responses, then closed their connections after headers and the first body chunk. All three downloads rejected, preserved the old destination and removed owned temporary files. The earlier arbitrary mock's clean HTTP200 with incomplete content is not evidence of a reachable application publication bug. MP3's ffmpeg/output errors terminate the response by code inspection; no MP3 interruption trial is claimed. Normal cancellation propagated promptly in the exercised paths; the documented forced-shutdown temporary-file limitation remains.

## 0.2.9 release wrap-up

The owner requested an explicit wrap-up, a GitHub release and a summary of the whole day's work. That request permits focused regression tests. Added three backup checks covering retained v2 output for small notebooks; v3 round-trip with repeated, changed and omitted historical snapshots, original/audio bytes and remapped IDs; and malformed hashes/amplification rejected before original publication. All 38 application tests pass.

v3 is now integrated. It is selected only when the normal v2 manifest exceeds 20 MiB. Canonical hashes cover complete source arrays, preserving distinct snapshots even when source IDs match. Bounds are 32 MiB per set, 101 sets and 128 MiB expanded source content; the manifest retains its 20 MiB ceiling. Total export size includes the manifest within the 2 GiB uncompressed limit. Import validates schema, ZIP membership, hashes, references and staged payloads before publishing files. Audio export keeps one streaming/cancellation path. Legacy v1/v2 imports remain supported.

A desktop source-build interaction saved a 28,054,791-byte synthetic notebook as a 29 KB ZIP and restored its current sources, four episodes, changed historical content and distinct IDs. Repetitive authored text compresses unusually well; this is an integrity check, not a representative compression claim. An initial diagnostic read the large response through the inspector cache and hit its body-size eviction; the corrected observation waits for the actual restore notice and compares content inside the renderer. Packaged startup, sandbox, private-backend access, persistence and restart passed. Final packaged download/import checks are recorded at the publication checkpoint below.

Publication checkpoint: 0.2.9 installer and portable are built. Packaged native Markdown/original/ZIP downloads, the 28 MB large-notebook restore, missing-original failure preserving the destination/screen, save-dialog cancellation and 390px/desktop download panels passed, with no renderer errors. Packaged empty/partial/oversize imports and filter resets passed. A post-restore diagnostic initially expected Settings to remain open, whereas the successful restore opens the new notebook's sources; reopening Settings corrected that selector. Packaged backend/bridge/HTML hashes match the checked source build; backend SHA-256 is `249ad0d94fd112766e44bab463e49d21bdc520b8d88b036af13d62bffccc6367`. Personal library data and credentials were not used for these checks or included in publication.
