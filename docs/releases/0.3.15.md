# LMBook 0.3.15

Local Windows iteration for the 0.4 workstream. Publication approval remains unset.

- Notebook **Note connections**: enable the background assistant for selected notes, choose ask/straightforward/full control, inspect exact evidence, accept/dismiss links and undo additions. Grouped in-app notices lead back to the review box.
- **Connections & settings → TypeSafe Jev**: connect/check/remove a direct TypeSafe key and independently enable notebook checks, link checks and search ranking. Luna continues through Codex.
- **Notebook checks**: selected source roles, possible groupings/disagreements, goal support, question answerability and saved-card quality, with bounded coverage, stale notices and Helpful/Not helpful feedback. These are experiments, not source verification.
- Meaning search optionally asks Jev to reorder its top ten matches, retaining all candidates and falling back to normal ordering on provider failure.
- Drag the bottom of workspace/review cards to change their height; keyboard controls and reset are available. Height choices persist.

Real Luna link behavior, all 38 existing application tests, production/desktop builds and the packaged Windows startup/sandbox/persistence/restart smoke passed. Jev integration was exercised with authored responses; the real key, API performance and judgment quality remain to be checked after connection. See [detailed behavior, evaluation and remaining limits](../ui/background-assistant.md) and [models used/not used](../research/background-assistant-model-usage.md).

Deliverables belong only in `D:\Downloads\SenniBook\release\0.3.15`. Native Mac checks, owner acceptance and 0.4 release approval remain pending.
